( sd.base-path -- flag )

.(      \ info, inside "data/base-path-equals.fth", `source-path`        is: ) cr
.(      \    ) source-path type cr
.(      \ info, inside "data/base-path-equals.fth", `source-base-path`   is: ) cr
.(      \    ) source-base-path type cr

source-base-path equals
